#include "mapa.h"

Mapa::Mapa()
{

}

 Mapa::Mapa(int altura, int largura){

  /*  Celula* celula = (Celula*) malloc(this->altura*sizeof(int));

    Celula** mapa = (Celula**) malloc(altura*largura*sizeof(int));

    int i = 0;
    for(; i < this->largura*this->altura; i++)
        mapa[i] = celula;
*/

}
